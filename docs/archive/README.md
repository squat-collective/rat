# Archived Documentation

Docs preserved for historical context. These describe past states of the
system; the live behaviour lives in the main `docs/` tree.

| File | Why archived |
|---|---|
| `fix-plan-2026-02.md` | Master fix plan generated from 7 code reviews in Feb 2026 (194 items). Most addressed across Waves 1-8 (commits `1c7482b` onward). Check the wave commit history for what shipped; this file is the snapshot of what we knew at the time. |

Files here are kept read-only — if any becomes useful for active work,
promote it back to `docs/` with a fresh edit pass first.
