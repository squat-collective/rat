// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: cloud/v1/cloud.proto

package cloudv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/rat-data/rat/platform/gen/cloud/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// CloudServiceName is the fully-qualified name of the CloudService service.
	CloudServiceName = "ratatouille.cloud.v1.CloudService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// CloudServiceGetCredentialsProcedure is the fully-qualified name of the CloudService's
	// GetCredentials RPC.
	CloudServiceGetCredentialsProcedure = "/ratatouille.cloud.v1.CloudService/GetCredentials"
)

// CloudServiceClient is a client for the ratatouille.cloud.v1.CloudService service.
type CloudServiceClient interface {
	// Vend scoped S3 credentials for a user within a namespace.
	// Returns temporary credentials (e.g., AWS STS AssumeRole) scoped to the
	// namespace's S3 prefix.
	GetCredentials(context.Context, *connect.Request[v1.GetCredentialsRequest]) (*connect.Response[v1.GetCredentialsResponse], error)
}

// NewCloudServiceClient constructs a client for the ratatouille.cloud.v1.CloudService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewCloudServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) CloudServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	cloudServiceMethods := v1.File_cloud_v1_cloud_proto.Services().ByName("CloudService").Methods()
	return &cloudServiceClient{
		getCredentials: connect.NewClient[v1.GetCredentialsRequest, v1.GetCredentialsResponse](
			httpClient,
			baseURL+CloudServiceGetCredentialsProcedure,
			connect.WithSchema(cloudServiceMethods.ByName("GetCredentials")),
			connect.WithClientOptions(opts...),
		),
	}
}

// cloudServiceClient implements CloudServiceClient.
type cloudServiceClient struct {
	getCredentials *connect.Client[v1.GetCredentialsRequest, v1.GetCredentialsResponse]
}

// GetCredentials calls ratatouille.cloud.v1.CloudService.GetCredentials.
func (c *cloudServiceClient) GetCredentials(ctx context.Context, req *connect.Request[v1.GetCredentialsRequest]) (*connect.Response[v1.GetCredentialsResponse], error) {
	return c.getCredentials.CallUnary(ctx, req)
}

// CloudServiceHandler is an implementation of the ratatouille.cloud.v1.CloudService service.
type CloudServiceHandler interface {
	// Vend scoped S3 credentials for a user within a namespace.
	// Returns temporary credentials (e.g., AWS STS AssumeRole) scoped to the
	// namespace's S3 prefix.
	GetCredentials(context.Context, *connect.Request[v1.GetCredentialsRequest]) (*connect.Response[v1.GetCredentialsResponse], error)
}

// NewCloudServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewCloudServiceHandler(svc CloudServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	cloudServiceMethods := v1.File_cloud_v1_cloud_proto.Services().ByName("CloudService").Methods()
	cloudServiceGetCredentialsHandler := connect.NewUnaryHandler(
		CloudServiceGetCredentialsProcedure,
		svc.GetCredentials,
		connect.WithSchema(cloudServiceMethods.ByName("GetCredentials")),
		connect.WithHandlerOptions(opts...),
	)
	return "/ratatouille.cloud.v1.CloudService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case CloudServiceGetCredentialsProcedure:
			cloudServiceGetCredentialsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedCloudServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedCloudServiceHandler struct{}

func (UnimplementedCloudServiceHandler) GetCredentials(context.Context, *connect.Request[v1.GetCredentialsRequest]) (*connect.Response[v1.GetCredentialsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("ratatouille.cloud.v1.CloudService.GetCredentials is not implemented"))
}
