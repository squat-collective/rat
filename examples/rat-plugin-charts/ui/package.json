{
  "name": "rat-plugin-charts-ui",
  "version": "0.2.0",
  "private": true,
  "description": "Portal UI bundle for the RAT charts plugin (Recharts, react-grid-layout, esbuild)",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "react-grid-layout": "^1.4.0",
    "recharts": "^2.12.0"
  }
}
