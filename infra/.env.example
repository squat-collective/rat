# RAT v2 — Environment Variables
# Copy to .env and fill in your values
#
# For local dev, the defaults in docker-compose.yml work out of the box.
# For production, override these with strong, unique credentials.

# Postgres
POSTGRES_USER=rat
POSTGRES_PASSWORD=changeme

# MinIO / S3
S3_ACCESS_KEY=changeme
S3_SECRET_KEY=changeme

# Optional
# RAT_API_KEY=
# TLS_CERT_FILE=
# TLS_KEY_FILE=
